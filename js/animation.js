
lottie.loadAnimation({
  container: document.getElementById('lottie-bg'),
  renderer: 'svg',       // 'svg' is most common
  loop: false,            // no loop
  autoplay: true,        // start automatically
  path: './animation/forest.json',  // path to Lottie JSON
  rendererSettings: {
    preserveAspectRatio: 'xMidYMid slice' // this makes it cover entire container
  }
});

lottie.loadAnimation({
  container: document.getElementById('lottie-bga'),
  renderer: 'svg',
  loop: false,            // no loop
  autoplay: true,        // start automatically
  path: './animation/sunrise.json',  // path to Lottie JSON
  rendererSettings: {
    preserveAspectRatio: 'xMidYMid slice' // this makes it cover entire container
  }
});

lottie.loadAnimation({
  container: document.getElementById('lottie-bgb'),
  renderer: 'svg',
  loop: true,            // no loop
  autoplay: true,        // start automatically
  path: './animation/sunrise.json',  // path to Lottie JSON
  rendererSettings: {
    preserveAspectRatio: 'xMidYMid slice' // this makes it cover entire container
  }
});

lottie.loadAnimation({
  container: document.getElementById('lottie-bgc'),
  renderer: 'svg',
  loop: false,            // no loop
  autoplay: true,        // start automatically
  path: './animation/sunrise.json',  // path to Lottie JSON
  rendererSettings: {
    preserveAspectRatio: 'xMidYMid slice' // this makes it cover entire container
  }
});

lottie.loadAnimation({
  container: document.getElementById('lottie-bgd'),
  renderer: 'svg',
  loop: false,            // no loop
  autoplay: true,        // start automatically
  path: './animation/sunrise.json',  // path to Lottie JSON
  rendererSettings: {
    preserveAspectRatio: 'xMidYMid slice' // this makes it cover entire container
  }
});